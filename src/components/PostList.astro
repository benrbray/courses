---
// import { getPosts } from "../util/query";
// import { getCollection } from 'astro:content';

interface Props {
  course: string,
}

const { } = Astro.props;
// const posts = await getPosts(post => {
//   const match = (post.data.course.id === course);
//   console.log("ID", post.data.course, match);

//   return false;
// })

// const _posts = await getCollection("post", (_post) => false);
---

<div>
NOTHING
<!-- {
  posts.map(post => {
    return (<div>Title:
      <a href={`${post.id}`}>{post.data.title}</a>
    </div>)
  })
} -->
<!-- {posts.map(post => {
  return (<div>
    <a href={f"${post.id}"}>{post.data.title}</a>
  </div>)
})} -->
</div>